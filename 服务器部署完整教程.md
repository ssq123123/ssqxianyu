# 闲鱼监控系统 - 服务器部署完整教程

## 🎯 部署概览

本教程将带您完成从零开始的服务器部署，包括：
- 服务器购买和基础配置
- 环境搭建（Node.js、MongoDB）
- 域名和SSL证书配置
- 应用部署和启动
- 监控和维护

## 📋 准备清单

在开始之前，您需要准备：
- [ ] 云服务器（推荐阿里云或腾讯云）
- [ ] 域名（已备案）
- [ ] 微信小程序账号
- [ ] 支付方式（服务器费用约300-500元/年）

---

## 第一步：购买和配置云服务器

### 1.1 选择云服务商

**推荐配置：**
- CPU: 2核
- 内存: 4GB
- 硬盘: 40GB SSD
- 带宽: 3Mbps
- 系统: Ubuntu 20.04 LTS

**推荐服务商：**

**阿里云 ECS:**
1. 访问：https://www.aliyun.com/product/ecs
2. 选择 "按量付费" 或 "包年包月"
3. 地域选择：华东1（杭州）或华北2（北京）
4. 实例规格：通用型 g6.large
5. 镜像：Ubuntu 20.04 64位
6. 网络：默认VPC，分配公网IP
7. 安全组：先使用默认，后续配置

**腾讯云 CVM:**
1. 访问：https://cloud.tencent.com/product/cvm
2. 配置类似阿里云
3. 选择 "标准型S5" 实例

### 1.2 安全组配置

**必须开放的端口：**
```
22    SSH登录
80    HTTP
443   HTTPS  
3000  应用端口（临时，后续用Nginx代理）
27017 MongoDB（仅内网访问）
```

**阿里云安全组设置：**
1. 进入ECS控制台 → 实例 → 更多 → 网络和安全组 → 安全组配置
2. 添加规则：
   - 授权策略：允许
   - 优先级：1
   - 协议类型：TCP
   - 端口范围：上述端口
   - 授权对象：0.0.0.0/0

### 1.3 连接到服务器

**Windows用户 - 使用Xshell或Putty:**

1. **下载Xshell（推荐）：**
   - 官网：https://www.xshell.com/zh/free-for-home-school/
   - 个人使用免费

2. **连接步骤：**
   ```
   主机：你的服务器公网IP
   端口：22
   用户名：ubuntu（或root）
   密码：购买时设置的密码
   ```

**Mac/Linux用户 - 使用终端：**
```bash
ssh ubuntu@你的服务器IP
# 输入密码后回车
```

---

## 第二步：服务器基础配置

### 2.1 更新系统
```bash
# 更新软件包列表
sudo apt update

# 升级已安装的软件包
sudo apt upgrade -y

# 安装常用工具
sudo apt install -y curl wget vim git htop unzip
```

### 2.2 创建普通用户（安全考虑）
```bash
# 创建新用户
sudo adduser nodeuser

# 添加sudo权限
sudo usermod -aG sudo nodeuser

# 切换到新用户
su - nodeuser
```

### 2.3 配置SSH密钥（可选，更安全）
```bash
# 在本地生成密钥对（Windows用Xshell的工具生成）
ssh-keygen -t rsa -b 4096

# 将公钥上传到服务器
mkdir -p ~/.ssh
chmod 700 ~/.ssh

# 将公钥内容添加到authorized_keys
# （通过复制粘贴或scp命令）
```

---

## 第三步：安装Node.js环境

### 3.1 安装Node.js 18.x LTS
```bash
# 添加NodeSource仓库
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -

# 安装Node.js
sudo apt install -y nodejs

# 验证安装
node --version  # 应显示 v18.x.x
npm --version   # 应显示 9.x.x
```

### 3.2 安装全局工具
```bash
# 安装PM2进程管理器
sudo npm install -g pm2

# 安装yarn（可选）
sudo npm install -g yarn

# 验证安装
pm2 --version
```

---

## 第四步：安装MongoDB数据库

### 4.1 添加MongoDB仓库
```bash
# 导入公钥
curl -fsSL https://pgp.mongodb.com/server-6.0.asc | sudo gpg -o /usr/share/keyrings/mongodb-server-6.0.gpg --dearmor

# 添加源列表
echo "deb [ arch=amd64,arm64 signed-by=/usr/share/keyrings/mongodb-server-6.0.gpg ] https://repo.mongodb.org/apt/ubuntu focal/mongodb-org/6.0 multiverse" | sudo tee /etc/apt/sources.list.d/mongodb-org-6.0.list
```

### 4.2 安装MongoDB
```bash
# 更新包列表
sudo apt update

# 安装MongoDB
sudo apt install -y mongodb-org

# 启动MongoDB
sudo systemctl start mongod
sudo systemctl enable mongod

# 验证安装
sudo systemctl status mongod
```

### 4.3 配置MongoDB安全
```bash
# 登录MongoDB
mongosh

# 创建管理员用户
use admin
db.createUser({
  user: "admin",
  pwd: "your_strong_password_here",
  roles: ["userAdminAnyDatabase"]
})

# 创建应用数据库用户
use xianyu_monitor
db.createUser({
  user: "xianyuuser",
  pwd: "your_app_password_here",
  roles: ["readWrite"]
})

# 退出
exit
```

### 4.4 启用MongoDB认证
```bash
# 编辑配置文件
sudo vim /etc/mongod.conf

# 找到security部分，取消注释并添加：
security:
  authorization: enabled

# 重启MongoDB
sudo systemctl restart mongod
```

---

## 第五步：安装Nginx（Web服务器）

### 5.1 安装Nginx
```bash
# 安装
sudo apt install -y nginx

# 启动
sudo systemctl start nginx
sudo systemctl enable nginx

# 测试
curl http://localhost
# 应该看到Nginx欢迎页面
```

### 5.2 配置防火墙
```bash
# 如果启用了UFW防火墙
sudo ufw allow 'Nginx Full'
sudo ufw allow OpenSSH
sudo ufw enable
```

---

## 第六步：域名和SSL配置

### 6.1 域名解析设置

在您的域名服务商管理面板中：
```
记录类型: A
主机记录: @ （或www）
记录值: 你的服务器公网IP
TTL: 600
```

等待DNS生效（通常5-10分钟）：
```bash
# 测试域名解析
nslookup your-domain.com
```

### 6.2 申请SSL证书（Let's Encrypt免费）
```bash
# 安装Certbot
sudo apt install -y certbot python3-certbot-nginx

# 申请证书（将your-domain.com替换为您的域名）
sudo certbot --nginx -d your-domain.com -d www.your-domain.com

# 测试自动续期
sudo certbot renew --dry-run
```

---

## 第七步：部署应用代码

### 7.1 克隆代码仓库
```bash
# 在用户主目录创建项目目录
cd ~
mkdir projects
cd projects

# 这里假设代码已上传到Git仓库
# git clone https://github.com/your-username/xianyu-monitor.git
# 或手动上传代码文件
```

### 7.2 安装依赖
```bash
cd xianyu-monitor/backend
npm install

# 如果遇到权限问题
sudo chown -R $USER ~/.npm
```

### 7.3 配置环境变量
```bash
# 创建环境配置文件
vim .env

# 添加以下配置：
PORT=3000
NODE_ENV=production
MONGODB_URI=mongodb://xianyuuser:your_app_password_here@localhost:27017/xianyu_monitor
JWT_SECRET=your_jwt_secret_here
WECHAT_APP_ID=your_wechat_app_id
WECHAT_APP_SECRET=your_wechat_app_secret
```

---

## 第八步：配置Nginx反向代理

### 8.1 创建站点配置
```bash
# 创建配置文件
sudo vim /etc/nginx/sites-available/xianyu-monitor

# 添加以下配置：
```

```nginx
server {
    listen 80;
    server_name your-domain.com www.your-domain.com;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name your-domain.com www.your-domain.com;

    ssl_certificate /etc/letsencrypt/live/your-domain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;

    # 应用接口代理
    location /api/ {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # WebSocket代理
    location /socket.io/ {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 静态文件
    location /uploads/ {
        alias /home/nodeuser/projects/xianyu-monitor/backend/public/uploads/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}
```

### 8.2 启用站点配置
```bash
# 创建软链接
sudo ln -s /etc/nginx/sites-available/xianyu-monitor /etc/nginx/sites-enabled/

# 删除默认站点
sudo rm /etc/nginx/sites-enabled/default

# 测试配置
sudo nginx -t

# 重载Nginx
sudo systemctl reload nginx
```

---

## 第九步：启动应用

### 9.1 使用PM2启动应用
```bash
cd ~/projects/xianyu-monitor/backend

# 启动应用
pm2 start server.js --name "xianyu-monitor"

# 设置开机自启
pm2 startup
pm2 save

# 查看应用状态
pm2 status
pm2 logs xianyu-monitor
```

### 9.2 配置PM2进程文件（推荐）
```bash
# 创建PM2配置文件
vim ecosystem.config.js
```

```javascript
module.exports = {
  apps: [{
    name: 'xianyu-monitor',
    script: 'server.js',
    instances: 2,
    exec_mode: 'cluster',
    env: {
      NODE_ENV: 'production',
      PORT: 3000
    },
    error_file: '/home/nodeuser/.pm2/logs/xianyu-monitor-error.log',
    out_file: '/home/nodeuser/.pm2/logs/xianyu-monitor-out.log',
    log_file: '/home/nodeuser/.pm2/logs/xianyu-monitor-combined.log',
    time: true
  }]
}
```

```bash
# 使用配置文件启动
pm2 start ecosystem.config.js
```

---

## 第十步：测试和验证

### 10.1 检查服务状态
```bash
# 检查Nginx状态
sudo systemctl status nginx

# 检查MongoDB状态
sudo systemctl status mongod

# 检查应用状态
pm2 status

# 检查端口占用
sudo netstat -tlnp | grep :443
sudo netstat -tlnp | grep :3000
```

### 10.2 测试API接口
```bash
# 测试基本连接
curl https://your-domain.com/api/health

# 查看应用日志
pm2 logs xianyu-monitor --lines 50
```

---

## 第十一步：监控和维护

### 11.1 设置日志轮转
```bash
# 创建日志轮转配置
sudo vim /etc/logrotate.d/xianyu-monitor

# 添加内容：
/home/nodeuser/.pm2/logs/*.log {
    daily
    missingok
    rotate 52
    compress
    notifempty
    create 644 nodeuser nodeuser
    postrotate
        pm2 reloadLogs
    endscript
}
```

### 11.2 设置系统监控
```bash
# 安装htop查看系统资源
sudo apt install htop

# 查看系统资源使用
htop

# 查看磁盘使用
df -h

# 查看内存使用
free -h
```

### 11.3 自动备份脚本
```bash
# 创建备份目录
mkdir -p ~/backups

# 创建备份脚本
vim ~/backup.sh
```

```bash
#!/bin/bash
DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_DIR="/home/nodeuser/backups"

# 备份数据库
mongodump --uri="mongodb://xianyuuser:your_app_password_here@localhost:27017/xianyu_monitor" --out="$BACKUP_DIR/mongo_$DATE"

# 压缩备份
tar -czf "$BACKUP_DIR/backup_$DATE.tar.gz" -C "$BACKUP_DIR" "mongo_$DATE"
rm -rf "$BACKUP_DIR/mongo_$DATE"

# 删除7天前的备份
find "$BACKUP_DIR" -name "backup_*.tar.gz" -mtime +7 -delete

echo "Backup completed: backup_$DATE.tar.gz"
```

```bash
# 设置执行权限
chmod +x ~/backup.sh

# 设置每日自动备份
crontab -e
# 添加：0 2 * * * /home/nodeuser/backup.sh
```

---

## 🚀 部署完成检查清单

完成后请验证：
- [ ] 服务器SSH连接正常
- [ ] Node.js和MongoDB正常运行
- [ ] 域名解析正确
- [ ] SSL证书申请成功
- [ ] Nginx配置正确
- [ ] 应用启动正常
- [ ] API接口可访问
- [ ] PM2自动重启设置
- [ ] 日志轮转配置
- [ ] 备份脚本设置

## 📞 遇到问题？

**常见问题和解决方案：**

1. **端口无法访问：**
   - 检查安全组设置
   - 检查防火墙规则
   - 确认服务正在运行

2. **SSL证书申请失败：**
   - 确认域名解析正确
   - 检查Nginx配置语法
   - 确认80端口可访问

3. **MongoDB连接失败：**
   - 检查用户名密码
   - 确认MongoDB服务运行
   - 检查网络连接

4. **应用启动失败：**
   - 查看PM2日志
   - 检查环境变量配置
   - 确认依赖安装完整

**获取帮助的命令：**
```bash
# 查看系统日志
sudo journalctl -f

# 查看Nginx日志
sudo tail -f /var/log/nginx/error.log

# 查看MongoDB日志
sudo tail -f /var/log/mongodb/mongod.log

# 查看应用日志
pm2 logs xianyu-monitor
```

---

现在您的服务器环境已经准备就绪！接下来我将为您创建完整的后端和小程序代码。

## 💡 重要提示

1. **密码安全：**
   - 使用强密码
   - 定期更换密码
   - 不要在代码中硬编码密码

2. **定期维护：**
   - 每周检查日志
   - 每月更新系统
   - 定期检查备份

3. **性能优化：**
   - 监控资源使用情况
   - 根据访问量调整PM2实例数
   - 考虑使用CDN加速静态资源

您现在准备好继续下一步了吗？我将开始创建完整的应用代码。
